import React from 'react'
import Popover from '@instructure/ui-core/lib/components/Popover'

export default function () {
	return (
		<Popover 
			closeButtonLabel="Close"
  		closeButtonRef={() => {}}
  		applicationElement={() => {}}
  	/>
	)
}